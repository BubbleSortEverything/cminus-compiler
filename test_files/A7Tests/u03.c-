// C-S21
main()
{
        int x;

        x=0;
        while x<8 do {
            output(x);
            if x<=1 then  outputc('A');
            else if x<=2 then  outputc('B');
            else if x<=3 then  outputc('C');
            else if x<=4 then  outputc('D');
            else outputc('E');
            outnl();

            output(x);
            if x<=1 then  outputc('A');
            else if x<=2 then  outputc('B');
            else if x<=3 then  outputc('C');
            else if x<=4 then  outputc('D');
            else outputc('E');
            outnl();

            x++;
            }
}
